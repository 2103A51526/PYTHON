# Regular function to add two numbers
def add(x, y):
    return x + y

# Lambda function to do the same
add_lambda = lambda x, y: x + y

# Using the lambda function
result = add_lambda(5, 3)
print(result)  # Output: 8
